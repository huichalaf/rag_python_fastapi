"""import sympy as sp

# Definir la variable y la función desconocida
t = sp.Symbol('t')
y = sp.Function('y')

# Definir la ecuación diferencial en términos de y(t) y sus derivadas
ecuacion_str = input("Ingresa la ecuación diferencial en términos de 't' y 'y': ")
ecuacion = sp.sympify(ecuacion_str)

# Resolver la ecuación diferencial
solucion = sp.dsolve(ecuacion, y(t))"""